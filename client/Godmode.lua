---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Lucas.
--- DateTime: 09/02/2021 18:39
---

-- Not Working (quando morre fica blipando)

--Citizen.CreateThread(function()
--    while true do
--        Citizen.Wait(60000)
--        local ped = PlayerPedId()
--
--        local current_health = GetEntityHealth(ped)
--        SetEntityHealth(ped,current_health - 2)
--
--        local random_wait = math.random(10,150)
--        Citizen.Wait(random_wait)
--
--        if not IsPlayerDead(ped) then
--            if PlayerPedId() == ped and GetEntityHealth(ped) == current_health and GetEntityHealth(ped) ~= 0 then
--                vPUNISHMENTS.applyPunish({ reason = "Godmode **(SUSPEITA III)**", punishment = "Log", requireScreenshoot = false });
--            elseif GetEntityHealth(ped) == current_health-2 then
--                SetEntityHealth(ped,GetEntityHealth(ped) + 2)
--            end
--        end
--    end
--end)